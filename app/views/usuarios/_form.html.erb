<%= simple_form_for(@usuario) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :admin %>
    <%= f.association :profissional %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
